import React from "react"
import * as Template from "../../ui/templates";

export default function NoMatch404() {
    return (
        <Template.Page>
            <div>Sorry there was page with that URL</div>
        </Template.Page>
    );
}